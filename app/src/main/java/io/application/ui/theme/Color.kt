package io.application.ui.theme

import androidx.compose.ui.graphics.Color

// ── 基底墨色 ──────────────────────────────────────────────────
val InkVoid        = Color(0xFF07050F)   // 最深背景
val InkDeep        = Color(0xFF0F0C1A)   // 主背景
val ParchmentNight = Color(0xFF1A1628)   // Surface

// ── 古金色系 ──────────────────────────────────────────────────
val GoldAncient = Color(0xFFCFA754)      // 主色：旧金
val GoldBright  = Color(0xFFEDD98A)      // 高亮金
val GoldDim     = Color(0xFF6B5120)      // 暗金（边框/装饰）
val GoldEmber   = Color(0xFFA07828)      // 炬光金

// ── 符文 & 魔法色 ─────────────────────────────────────────────
val RuneBlue    = Color(0xFF78C0DC)      // 魔力蓝
val RuneViolet  = Color(0xFFA87ED4)      // 奥术紫
val RuneGreen   = Color(0xFF68A878)      // 自然绿

// ── 文字色 ────────────────────────────────────────────────────
val ScrollText      = Color(0xFFE8D9B8)  // 主文字（羊皮纸白）
val ScrollTextDim   = Color(0xFF9A8D70)  // 次文字
val ScrollTextFaint = Color(0xFF4E4535)  // 弱文字

// ── 状态色 ────────────────────────────────────────────────────
val BloodHP    = Color(0xFFC0392B)       // 生命红
val SapphireMP = Color(0xFF2E86C1)       // 法力蓝
val XPAmber    = Color(0xFFE8A020)       // 经验琥珀

// ── 副本区域色种子 ─────────────────────────────────────────────
val ZoneForest  = Color(0xFF2D5A27)      // 黑暗森林
val ZoneAbyss   = Color(0xFF2A1550)      // 深渊裂缝
val ZoneTemple  = Color(0xFF3A3260)      // 银月神殿
val ZoneRuins   = Color(0xFF4A2010)      // 诅咒遗址
val ZoneGlacier = Color(0xFF1A3A5C)      // 冰封绝峰
